# FundFlow
Built a client-side web app using vanilla JavaScript with Local Storage for persistent data saving. Implemented a dynamic balance engine calculating income and expenses against a base salary. Added core features like transaction entry, total expense calculation, and data reset functionality.
